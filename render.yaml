services:
  - type: web
    name: slopdetector
    runtime: docker
    dockerfilePath: ./Dockerfile
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: SLOP_MODEL_PATH
        value: training/best_model.pt
